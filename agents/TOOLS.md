---
description: The tools that the agent has access to and how to use them.
---
#DRAFT

You have access to the `docs-embeddings-pipeline`, which collect documents, converts them into searchable vectors, adds structured metadata, and makes them ready for semantic discovery.